{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agent-ix.org/agent-ix/spec-artifacts-app/0.2.0/Verification.json",
  "type": "object",
  "properties": {
    "method": {
      "$ref": "https://schemas.agent-ix.org/agent-ix/spec-artifacts-app/0.2.0/NonEmptyText.json",
      "description": "free text: an ISO 29148 method name whose vocabulary is an advisory lint rule, not a schema."
    },
    "testRefs": {
      "type": "array",
      "items": {
        "$ref": "https://schemas.agent-ix.org/agent-ix/spec-artifacts-app/0.2.0/TestCaseRef.json"
      }
    },
    "annotation": {
      "$ref": "https://schemas.agent-ix.org/agent-ix/spec-artifacts-app/0.2.0/NonEmptyText.json",
      "description": "free text: the parenthesized remainder of the cell, carried verbatim so no byte is lost."
    }
  },
  "required": [
    "method",
    "testRefs"
  ],
  "unevaluatedProperties": {
    "not": {}
  },
  "description": "A verification cell split into its parts. For `<method> (<annotation>)`,\n`method` is the text before the first `(`, `annotation` the text between the\nfirst `(` and the last `)` verbatim, and `testRefs` the `TC-N` tokens found\nin `annotation`, in order. No byte of the cell is dropped."
}
